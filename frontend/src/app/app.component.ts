import { Component } from '@angular/core';

/** App shell: the router outlet plus the global toast stack. */
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
})
export class AppComponent {}
