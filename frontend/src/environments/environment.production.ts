export const environment = {
  production: true,
  // Production serves the API behind the same origin. A reverse proxy can
  // route /api to ASP.NET without baking a deployment hostname into the bundle.
  apiUrl: '/api',
};
